---
description: 代码清晰规范优先；优化只关注时间复杂度，不做常数级微优化
alwaysApply: true
---

# 代码风格与优化原则

为用户编写代码时，**清晰、规范、可读性优先**。

## 优化范围

- **只做算法层面的优化**：时间复杂度（必要时兼顾空间复杂度），如 O(n²) → O(n log n)、O(n) 单遍扫描等
- **不做常数级微优化**：不为 LeetCode 运行时间排名牺牲可读性（如全内联、下标 hack、`ord()` 替代 `int()`、提前溢出 return 等）

## 优先

- 逻辑分步清晰，必要时拆成有意义的辅助函数
- 命名直观，结构易读、易调试
- 与现有文件风格保持一致
- 讨论性能时，聚焦「能不能用更优的算法」，而非「能不能少几毫秒」

## 避免（除非用户要求）

- 为 beat % 或常数因子改写可读代码
- 主动提供「更快但难读」的替代版本
- 因 LeetCode 排名低而建议微优化
